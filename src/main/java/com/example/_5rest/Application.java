package com.example._5rest;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class Application {

    public static void main(String[] args) {

        SpringApplication.run(Application.class, args);
    }
    /*
 Suppose you need to design a REST API for a super simple stateless unsecured personal notes app
A user just provides their first name, last name
A note is just a piece of text with a title
Implement an REST API endpoints for the following functionality:
Add a new user
Retrieve user’s data
Add a user's note
Remove a user
Expose your REST API using ngrok (optional)
Post  screenshots of your code and the exposed URL in the Thread
     */

}
